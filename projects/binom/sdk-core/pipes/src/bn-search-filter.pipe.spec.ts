import { BnSearchFilterPipe } from './bn-search-filter.pipe';

describe('BnSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BnSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
