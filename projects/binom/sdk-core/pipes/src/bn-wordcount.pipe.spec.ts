import { BnWordcountPipe } from './bn-wordcount.pipe';

describe('BnWordcountPipe', () => {
  it('create an instance', () => {
    const pipe = new BnWordcountPipe();
    expect(pipe).toBeTruthy();
  });
});
