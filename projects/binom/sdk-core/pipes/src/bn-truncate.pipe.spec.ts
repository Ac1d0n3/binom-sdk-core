import { BnTruncatePipe } from './bn-truncate.pipe';

describe('BnTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new BnTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
