import { BnHighlightPipe } from './bn-highlight.pipe';

describe('BnHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new BnHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
