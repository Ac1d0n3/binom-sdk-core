import { BnReplacePipe } from './bn-replace.pipe';

describe('BnReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new BnReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
