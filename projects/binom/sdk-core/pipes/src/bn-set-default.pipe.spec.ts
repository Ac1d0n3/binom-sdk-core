import { BnSetDefaultPipe } from './bn-set-default.pipe';

describe('BnSetDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new BnSetDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
